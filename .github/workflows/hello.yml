name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "Hello world!"
      - name: Tell the date
        run: date
      - name: print directory contents in long format
        run: ls -l