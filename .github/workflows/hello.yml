name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello world!"
      - name: Tell the date
        run: date
      - name: print directory contents in long format
        run: ls -l